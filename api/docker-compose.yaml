services:
  app:
    build: .
    command: uvicorn app.app:app --host 0.0.0.0 --port 5000 --reload
    volumes:
      - .:/app
    ports:
      - "5000:5000"
    container_name: hackathon_indesia
    env_file:
      - .env
